import React from "react";

const Button = () => {
  return (
    <button className="text-white p-2 rounded-md bg-blue-500 h-10 text-[#ececec]">
      Button nef
    </button>
  );
};

export default Button;
